[package]
name = "fonts"
version = "0.1.0"
entrypoint = "main.typ"
description = "Example Oicana template using custom fonts."

[tool.oicana]
manifest_version = 1

[[tool.oicana.inputs]]
type = "json"
key = "data"
default = "sample.json"
schema = "schema.json"
