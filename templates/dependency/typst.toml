[package]
name = "dependency"
version = "0.1.0"
entrypoint = "main.typ"
description = "An example Oicana template using a rather render-heavy third party dependency."

[tool.oicana]
manifest_version = 1

[[tool.oicana.inputs]]
type = "json"
key = "data"
default = "sample.json"
schema = "schema.json"
